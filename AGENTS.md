# AGENTS.md — とむSYSTEM

このファイルはClaude Codeへの作業指示書です。

## プロジェクト概要

- `index.html` : フロントエンド（全アプリUI）
- `tomu_system_worker.js` : Cloudflare Worker（APIプロキシ・認証）
- `apps.json` : アプリ定義ファイル
- デプロイ: `deploy.ps1` または `deploy_wrangler.ps1`

## 現在のタスク: A-6〜A-11

以下を **index.html** を中心に実装すること。変更は最小限・既存コードスタイルに合わせること。

---

### A-6: ストリーミング対応

- Anthropic APIのストリーミングレスポンス（`stream: true`）を使い、回答を逐次表示する
- Workerside（`tomu_system_worker.js`）でSSEをフォワードし、フロント側でReadableStreamを受け取って表示

### A-7: XSSエスケープ強化

- `innerHTML` に直接ユーザー入力やAIレスポンスを挿入している箇所をすべて特定する
- テキストは `textContent` または適切なエスケープ関数を通じて挿入する
- Markdownレンダリングが必要な箇所はサニタイズ済みのものだけ `innerHTML` に渡す

### A-8: 会話履歴トリミング

- 会話履歴が長くなりすぎた場合にトークン超過エラーが起きないよう、古いメッセージから削除する
- 直近N件（例: 20件）または合計文字数上限（例: 40,000文字）を超えたら先頭から削除する

### A-9: 新しい相談ボタン

- 各アプリ画面に「新しい相談を始める」ボタンを追加する
- クリックで会話履歴をリセットし、初期状態に戻る
- 確認ダイアログは不要（即リセット）

### A-10: エラー時リトライボタン

- API呼び出しが失敗した場合、エラーメッセージと共に「再試行」ボタンを表示する
- 再試行ボタンをクリックすると同じリクエストを再送する

### A-11: アクセシビリティ改善

- ボタン・入力欄に適切な `aria-label` を付与する
- キーボード操作（Tab移動・Enter送信）が正しく動作するか確認・修正する
- コントラスト比が低い箇所があれば修正する

---

## 作業方針

1. タスクは A-6 → A-11 の順に実装する
2. 各タスク完了後にコミットする（例: `feat: A-6 ストリーミング対応`）
3. デプロイは手動で行うため、コード変更のみでOK
4. 不明点があれば実装を止めてコメントに残す

---

## 開発環境セットアップ（必須・clone直後に1回）

```sh
git config core.hooksPath .githooks
```

これで pre-commit hook が有効になり、テスト用モック／認証バイパス
（LOCAL TEST MOCK・`window.TomuAuth = {...}` の差し替え等）の混入を
commit 時点でブロックする。

## テストモック混入ガード（3層）

フロントは GitHub Pages 直配信（git push = 即公開）のため、認証バイパスを
含むテスト用コードが本番に混入した事故（P0-1, 2026-07-06修正）の再発防止
として以下の3層で検査している。検査本体は `scripts/check-no-test-mock.mjs`。

| 層 | タイミング | 役割 |
| --- | --- | --- |
| `.githooks/pre-commit` | commit 時 | 主防壁。push 前にブロック |
| `.github/workflows/check-no-test-mock.yml` | push / PR 時 | 保険。混入時に CI 失敗で即検知（公開自体は止まらない） |
| `deploy_wrangler.ps1` | Worker デプロイ時 | Worker 側の最終ゲート |

- ローカルテスト用の認証モックは **アプリの index.html に直接書かないこと**。
  必要なら未追跡の別ファイル（.gitignore 対象）に置いて手元でだけ読み込む。
- `git commit --no-verify` はガードを素通りするため緊急時以外禁止
  （その場合も Actions が push 後に検知する）。
- 意図的な例外は該当行末に `mock-allow` を付ける（原則使わない）。
